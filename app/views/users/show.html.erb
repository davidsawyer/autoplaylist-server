<p id="notice"><%= notice %></p>

<p>
  <strong>Uri:</strong>
  <%= @user.uri %>
</p>

<p>
  <strong>Event:</strong>
  <%= @user.event_id %>
</p>

<p>
  <strong>Tracks:</strong>
  <%= @user.tracks.map(&:uri).join(', ') %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
